# pyproject.toml

# Note: Many fields are autogenerated by 'WIPACrepo/wipac-dev-py-setup-action'.
#       See this repo's GitHub Actions workflow(s) (.github/workflows/*.yaml)
#       and https://github.com/WIPACrepo/wipac-dev-py-setup-action for details.

[build-system] # do not edit — autogenerated by wipac-dev-py-setup-action
requires = ["setuptools>=78.1", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
    'Flask>=3.1.3',
    'Werkzeug>=3.1.8',
    'cachetools>=7.2.0',
    'coloredlogs>=15.0.1',
    'dacite>=1.9.2',
    'dash-bootstrap-components>=2.0.4',
    'dash>=4.4.1',
    'flask-oidc>=2.4.0',
    'motor>=3.7.1',
    'openpyxl>=3.1.5',
    'pandas>=3.0.6',
    'pymongo>=4.17.0,<4.19',
    'python-dateutil>=2.9.0.post0',
    'requests>=2.34.2',
    'tornado>=6.5.10',
    'typeguard>=4.6.0',
    'visdcc>=0.0.63',
    'wipac-dev-tools>=1.21.0',
    'wipac-keycloak-rest-services>=1.5.7',
    'wipac-rest-tools>=1.13.5',
]
dynamic = ["version"] # do not edit — autogenerated by wipac-dev-py-setup-action
name = "rest-server-web-app-universal-utils" # do not edit — autogenerated by wipac-dev-py-setup-action
description = "A front-end to allow PIs to easily report to the ICC their Statements of Work in accordance with MoUs: https://mou.icecube.aq" # do not edit — autogenerated by wipac-dev-py-setup-action
readme = "README.md" # do not edit — autogenerated by wipac-dev-py-setup-action
keywords = [
    'WIPAC',
    'IceCube',
] # do not edit — autogenerated by wipac-dev-py-setup-action
classifiers = [
    'Programming Language :: Python :: 3.11',
    'Programming Language :: Python :: 3.12',
    'Programming Language :: Python :: 3.13',
] # do not edit — autogenerated by wipac-dev-py-setup-action
requires-python = ">=3.11, <3.14" # do not edit — autogenerated by wipac-dev-py-setup-action
license = "MIT" # do not edit — autogenerated by wipac-dev-py-setup-action

[project.optional-dependencies]
tests = [
    'nest-asyncio',
    'pytest',
    'pytest-asyncio',
    'pytest-mock',
]
mypy = [
    'nest-asyncio',
    'pytest',
    'pytest-asyncio',
    'pytest-mock',
] # do not edit — autogenerated by wipac-dev-py-setup-action

[project.urls] # do not edit — autogenerated by wipac-dev-py-setup-action
Homepage = "https://github.com/WIPACrepo/mou-dashboard" # do not edit — autogenerated by wipac-dev-py-setup-action
Tracker = "https://github.com/WIPACrepo/mou-dashboard/issues" # do not edit — autogenerated by wipac-dev-py-setup-action
Source = "https://github.com/WIPACrepo/mou-dashboard" # do not edit — autogenerated by wipac-dev-py-setup-action

[tool.setuptools] # do not edit — autogenerated by wipac-dev-py-setup-action
packages = [
    'rest_server',
    'rest_server.data_sources',
    'rest_server.utils',
    'universal_utils',
    'web_app',
    'web_app.contents',
    'web_app.data_source',
    'web_app.utils',
] # do not edit — autogenerated by wipac-dev-py-setup-action

[tool.setuptools.package-data]
"*" = ["py.typed"] # do not edit — autogenerated by wipac-dev-py-setup-action

[tool.setuptools_scm] # do not edit — autogenerated by wipac-dev-py-setup-action
fallback_version = "CANNOT_BUILD_WITHOUT_GIT_DIR" # used when SCM metadata is missing (no '.git/') — if '.git/' exists but does not contain tags, a commit-based version (like '0.1.dev1+g79c9b4b') will be used instead.

[tool.ty]

[tool.ty.src]
exclude = [
    "tests/**",
    "resources/**",
]

[tool.ty.rules]
# Downgraded to warnings (not ignored) rather than fixed outright: `dash>=4.4.1` now ships
# real type stubs (py.typed), where the previously-pinned `dash==2.16.1` did not, so the
# `# type: ignore[import]` comments on `import dash` used to blanket-silence these as `Any`
# under mypy. ty now sees real Dash types and surfaces pre-existing mismatches across the
# web_app callback modules (Dash callbacks declaring e.g. `-> bool`/`-> str` but actually
# receiving `Output(...)`/`Input(...)`/`State(...)` descriptor objects at decoration time,
# and returning dash's `NoUpdate` sentinel where a concrete type is declared) — a real but
# separate follow-up (fixing the Dash callback typing scheme repo-wide), not part of this
# CI/packaging migration. Kept as warnings (not "ignore") so they stay visible in `ty check`
# output for that follow-up, while not failing CI (lint-python.yml runs
# `ty check --exit-zero-on-warning`).
#
# NOTE: a few of these, outside the Dash-callback pattern, look like they could be genuine
# latent bugs rather than Dash-stub noise and are worth a closer look independently of the
# broader cleanup: rest_server/data_sources/mou_db.py:578,589,604 and
# rest_server/utils/utils.py:246 index/assign `dict[str, ...]` using `ObjectId | float`
# values sourced from Mongo documents, where a `str` key is expected.
invalid-argument-type = "warn"
invalid-assignment = "warn"
invalid-return-type = "warn"
invalid-method-override = "warn"

[tool.pytest.ini_options]

[tool.ruff]
exclude = ["tests", "resources"]

[tool.ruff.lint]
# ruff's default rule selection is very broad (~900 rules); iceprod's pyproject.toml
# (our reference implementation) ignores whole noisy categories to approximate the old
# flake8 scope (pyflakes + pycodestyle) plus E501 (matching this repo's prior
# `flake8-ignore = E501 W503`, W503 has no ruff equivalent since ruff follows PEP 8's
# current stance on line-break-before-binary-operator).
ignore = ["BLE001", "C408", "DTZ", "E501", "LOG015", "PIE", "PLC0206", "RUF", "S", "SIM", "TRY", "UP"]
