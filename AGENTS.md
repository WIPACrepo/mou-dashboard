# AGENTS.md

This is a Python project, with the only output being an install in a container.

## Project Overview

See `README.md` for a project overview.

## Tests

* Tests live in the `tests` directory.
* Assume a MongoDB server is already running locally.
